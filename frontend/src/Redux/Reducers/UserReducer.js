import {} from "../Actions/UserActions" // importar las action types
import {} from "../Actions/UserActions" // importar las actions



const initialStateOfCUser = { 
    
}

function UserReducer(state = initialStateOfCUser, { type, payload }) {
    // eslint-disable-next-line default-case
    switch (type) {


        default:
            return {...state}
    }
}
export default UserReducer