/*import axios from "axios";
import { useEffect, useState } from "react";
import { getCharacterByName } from "../../../Redux/Actions/actions";
import CharacterCard from "../../Cards/CharacterCard/CharacterCard";

const CharactersIcon = () => {

  const [type, setType] = useState([ ]);
    const [selectedCharacter, setSelectedCharacter] = useState([]);
    const [page, setPage] = useState(1);
    const [content, setContent] = useState([]);
    const [numOfPages, setNumOfPages] = useState();
  
return(
  <div>
    <span className="CharactersIcon">CharactersIcon</span>
  </div>
);
};

export default CharactersIcon;*/